version: "3.8"

services:
  app:
    container_name: lookup2.0
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev
    restart: unless-stopped

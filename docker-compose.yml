version: "3.8"

services:
  app:
    container_name: express-ts-app
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev
    restart: unless-stopped
